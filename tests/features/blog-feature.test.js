import { describe, expect, it } from "@jest/globals";

describe("when admin create a blog", () => {
  it("should be able to create a blog successfully", async () => {
    expect(true).toBe(true);
  });
});
