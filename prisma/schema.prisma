// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Admin {
  id          String  @id @default(uuid()) @db.VarChar(255)
  name        String  @db.VarChar(255)
  password    String  @db.VarChar(255)
  email       String  @unique @db.VarChar(255)
  phoneNumber String  @unique @map("phone_number") @db.VarChar(255)
  role        Role
  token       String? @unique @db.VarChar(255)

  blogs       Blog[]
  careers     Career[]
  permissions AdminPermission?

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("admins")
}

model AdminPermission {
  id Int @id @default(autoincrement())

  adminId String @unique @map("admin_id") @db.VarChar(255)
  admin   Admin  @relation(fields: [adminId], references: [id])

  canShowBlog Boolean @default(false) @map("can_show_blog")

  canCreateBlog Boolean @default(false) @map("can_create_blog")
  canViewBlog   Boolean @default(false) @map("can_view_blog")
  canUpdateBlog Boolean @default(false) @map("can_update_blog")
  canDeleteBlog Boolean @default(false) @map("can_delete_blog")

  canShowCareer Boolean @default(false) @map("can_show_career")

  canCreateCareer Boolean @default(false) @map("can_create_career")
  canViewCareer   Boolean @default(false) @map("can_view_career")
  canUpdateCareer Boolean @default(false) @map("can_update_career")
  canDeleteCareer Boolean @default(false) @map("can_delete_career")

  canShowApplication Boolean @default(false) @map("can_show_application")

  canCreateApplication Boolean @default(false) @map("can_create_application")
  canViewApplication   Boolean @default(false) @map("can_view_application")
  canUpdateApplication Boolean @default(false) @map("can_update_application")
  canDeleteApplication Boolean @default(false) @map("can_delete_application")

  canShowProduct Boolean @default(false) @map("can_show_product")

  canCreateProduct Boolean @default(false) @map("can_create_product")
  canViewProduct   Boolean @default(false) @map("can_view_product")
  canUpdateProduct Boolean @default(false) @map("can_update_product")
  canDeleteProduct Boolean @default(false) @map("can_delete_product")

  canShowAdmin Boolean @default(false) @map("can_show_admin")

  canCreateAdmin Boolean @default(false) @map("can_create_admin")
  canViewAdmin   Boolean @default(false) @map("can_view_admin")
  canUpdateAdmin Boolean @default(false) @map("can_update_admin")
  canDeleteAdmin Boolean @default(false) @map("can_delete_admin")

  @@map("admin_permissions")
}

enum Role {
  ADMIN
  SUPER_ADMIN
}

model Blog {
  id      String   @id @default(uuid()) @db.VarChar(255)
  title   String   @db.VarChar(255)
  content String   @db.Text
  slug    String   @unique @db.VarChar(255)
  type    BlogType
  photo   String   @db.VarChar(255)

  isVisible Boolean @default(false) @map("is_visible")

  authorId String @map("author_id") @db.VarChar(255)
  author   Admin  @relation(fields: [authorId], references: [id])

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("blogs")
}

enum BlogType {
  TECH
  BUSINESS
  NEWS
  TUTORIAL
  OTHER
}

model Career {
  id            Int        @id @default(autoincrement())
  title         String     @db.VarChar(255)
  description   String     @db.Text
  salary        String     @db.VarChar(255)
  tags          String     @db.Text
  type          CareerType
  linkedInInfo  String     @map("linkedin_info") @db.VarChar(255)
  glintsInfo    String     @map("glints_info") @db.VarChar(255)
  jobStreetInfo String     @map("jobstreet_info") @db.VarChar(255)

  authorId String @map("author_id") @db.VarChar(255)
  author   Admin  @relation(fields: [authorId], references: [id])

  applications Application[]

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("careers")
}

model Application {
  id              String               @id @default(uuid()) @db.VarChar(255)
  applicantName   String               @map("applicant_name") @db.VarChar(255)
  email           String               @db.VarChar(255)
  phoneNumber     String               @map("phone_number") @db.VarChar(255)
  educationType   EducationType        @map("education_type")
  instituteName   String               @map("institute_name") @db.VarChar(255)
  companyName     String?              @map("company_name") @db.VarChar(255)
  position        String?              @db.VarChar(255)
  lengthOfService LengthOfServiceType? @map("length_of_service")
  skills          String               @db.Text
  file            String               @map("file") @db.VarChar(255)
  letter          String               @db.Text

  careerId Int    @map("career_id")
  career   Career @relation(fields: [careerId], references: [id])

  createdAt DateTime @default(now()) @map("created_at")

  @@map("applications")
}

enum LengthOfServiceType {
  BELOW_1_YEAR
  ABOVE_1_YEAR
}

enum CareerType {
  UI_UX
  WEB
  MOBILE
  DESKTOP
  SYSTEM_ANALYST
  QUALITY_ASSURANCE
  DATA_ANALYST
  GENERAL_AFFAIR
  MARKETING
  ACCOUNTING
}

enum EducationType {
  SD
  SMP
  SMA
  SMK
  D3
  D4
  S1
  S2
  S3
}

model Inquiry {
  id         String @id @default(uuid()) @db.VarChar(255)
  senderName String @map("sender_name") @db.VarChar(255)
  email      String @db.VarChar(255)
  subject    String @db.VarChar(255)
  message    String @db.Text

  createdAt DateTime @default(now()) @map("created_at")

  @@map("inquiries")
}

model Product {
  id          Int     @id @default(autoincrement())
  title       String  @db.VarChar(255)
  description String  @db.Text
  mainFeature String  @map("main_feature") @db.Text
  advantage   String  @db.Text
  mainPhoto   String?  @db.VarChar(255)
  secondPhoto String? @db.VarChar(255)
  thirdPhoto  String? @db.VarChar(255)

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("products")
}
